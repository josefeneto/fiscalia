# ============================================
# FISCALIA - Configuração Mínima
# ============================================
#
# Copie este arquivo para .env e configure apenas o essencial
# Todo o resto usa valores padrão inteligentes
#

# ==================== OBRIGATÓRIO ====================
# Configure PELO MENOS UMA destas opções:

# Opção 1: Groq (recomendado - gratuito)
GROQ_API_KEY=your-groq-key-here
# GROQ_MODEL=groq/llama-3.3-70b-versatile  # (opcional - já é o padrão)

# Opção 2: OpenAI
# OPENAI_API_KEY=your-openai-key-here
# OPENAI_MODEL=gpt-4o-mini  # (opcional - já é o padrão)


# ==================== RAILWAY (PRODUÇÃO) ====================
# Variáveis que o Railway precisa:

# Database PostgreSQL (recomendado para Railway)
# Adicione um PostgreSQL Plugin no Railway e ele gera automaticamente
# DATABASE_URL=postgresql://user:password@host:port/database

# O Railway já configura automaticamente:
# - PORT (porta do serviço)
# - RAILWAY_ENVIRONMENT (production)
# - RAILWAY_VOLUME_MOUNT_PATH (/data)


# ==================== OPCIONAL ====================
# Só configure se quiser mudar os padrões

# Limites de processamento (valores padrão já funcionam bem)
# MAX_FILES_PER_BATCH=100
# MAX_FILE_SIZE_MB=50
# PROCESSING_TIMEOUT=600

# Ambiente (detecta automaticamente Railway vs local)
# ENVIRONMENT=development  # ou production


# ==================== NÃO PRECISA CONFIGURAR ====================
# Estes são detectados/configurados automaticamente:
# 
# - Paths de arquivos (criados automaticamente)
# - Database SQLite (fallback se não houver PostgreSQL)
# - Streamlit port/address (defaults ou Railway PORT)
# - Logging level (INFO em prod, DEBUG em dev)
# - Railway volume mount (detectado via RAILWAY_VOLUME_MOUNT_PATH)
#
# O sistema funciona "out of the box" com apenas a API key!


# ==================== QUICK START ====================
# 
# 1. DESENVOLVIMENTO LOCAL:
#    - Copie este arquivo: cp .env.example .env
#    - Adicione sua GROQ_API_KEY
#    - Rode: streamlit run app.py
#    - Pronto! ✅
#
# 2. RAILWAY (PRODUÇÃO):
#    a) Crie um novo projeto no Railway
#    b) Adicione um PostgreSQL Plugin (opcional mas recomendado)
#    c) No serviço principal, configure APENAS:
#       * GROQ_API_KEY=sua-chave-aqui
#       * (DATABASE_URL é auto-configurado pelo PostgreSQL Plugin)
#    d) Deploy automático via GitHub!
#    e) Railway.app cria URL público automaticamente 🚀
#
# 3. RAILWAY - CHECKLIST:
#    ✅ railway.json configurado
#    ✅ Dockerfile otimizado
#    ✅ Volume /data montado
#    ✅ Healthcheck ativo
#    ✅ Build otimizado com cache
#    ✅ PostgreSQL opcional
#    ✅ Restart automático


# ==================== TROUBLESHOOTING ====================
#
# Erro: "No API key found"
# → Adicione GROQ_API_KEY ou OPENAI_API_KEY
#
# Erro: "Database connection failed" 
# → Verifique DATABASE_URL ou deixe usar SQLite
#
# Erro: "Port already in use"
# → Railway configura PORT automaticamente
#
# Logs não aparecem:
# → Railway mostra logs em tempo real no dashboard